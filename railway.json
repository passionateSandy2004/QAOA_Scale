{
  "build": {
    "builder": "dockerfile",
    "dockerfilePath": "Dockerfile.prod"
  },
  "deploy": {
    "restartPolicyType": "always",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 100
  }
}